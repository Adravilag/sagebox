{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "stencil.config.ts", "tsconfig.json", "package.json"],
      "outputs": ["dist/**", "loader/**"]
    },
    "build:angular": {
      "dependsOn": ["build"],
      "outputs": ["dist/angular/**"]
    },
    "build:react": {
      "dependsOn": ["build"],
      "outputs": ["react/dist/**"]
    },
    "build:all": {
      "dependsOn": ["build", "build:angular", "build:react"],
      "outputs": ["dist/**", "loader/**", "react/dist/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "test:spec": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "start": {
      "cache": false,
      "persistent": true
    },
    "docs:dev": {
      "cache": false,
      "persistent": true
    },
    "docs:build": {
      "dependsOn": ["build"],
      "outputs": ["dist-docs/**"]
    }
  }
}
